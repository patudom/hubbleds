version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install --upgrade pip
  pre_build:
    commands:
      - pip install .
  build:
    commands:
      - echo Build completed.
artifacts:
  files:
    - '**/*'
  discard-paths: no
